export const DEPENDENCY_TYPES = [
	'dependencies',
	'devDependencies',
	'peerDependencies',
	'optionalDependencies',
] as const;

export const SUPPORTED_VERSION_PREFIXES = ['>=', '<=', '>', '<', '~', '^'];
